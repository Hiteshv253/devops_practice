ps aux | grep 4212
sudo kill -9 4212
ps aux | grep -E 'apt|dpkg'
sudo kill -9 <PID>
sudo rm /var/lib/dpkg/lock-frontend
sudo rm /var/lib/dpkg/lock
sudo rm /var/cache/apt/archives/lock
sudo dpkg --configure -a
sudo apt update


###Install AWS cli for secrite
sudo apt install awscli

###set access keys
aws configure 

###Credentials File:
The AWS CLI stores your credentials in a configuration file (usually ~/.aws/credentials

###check access key are working or not(IN ubantu
aws sts get-caller-identity


##List AWS Services:
###Lists all IAM users. 
aws iam list-users: 

###Lists all IAM roles. 
aws iam list-roles: 

###EC2 (Elastic Compute Cloud):
aws ec2 describe-instances: ##Lists all EC2 instances. 

###S3 (Simple Storage Service):
aws s3 ls: ###Lists all S3 buckets. 

###CloudFormation:
###Lists all CloudFormation stacks. 
aws cloudformation describe-stacks: 
